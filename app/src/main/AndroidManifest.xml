<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.milica.privatnabiblioteka">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".BookListActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".AuthorListActivity" />
        <activity android:name=".GenreListActivity" />
        <activity android:name=".AddBook" />
        <activity android:name=".UpdateBookActivity" />
        <activity android:name=".AddAuthor" />
        <activity android:name=".UpdateAuthorActivity" />
        <activity android:name=".UpdateGenreActivity" />
        <activity android:name=".AddGenre" />
        <activity android:name=".BookApiActivity"></activity>
    </application>

</manifest>